import { RouteSkeleton } from "../_components/route-skeleton";

export default function SearchPage() {
  return (
    <RouteSkeleton
      title="Search"
      description="Cross-library search route skeleton for tracks, albums, artists, and playlists."
    />
  );
}
