import { RouteSkeleton } from "../_components/route-skeleton";

export default function PlayersPage() {
  return (
    <RouteSkeleton
      title="Players"
      description="Player orchestration foundation for queue, transport controls, and synchronized playback states."
    />
  );
}
