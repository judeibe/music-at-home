import { RouteSkeleton } from "./_components/route-skeleton";

export default function HomePage() {
  return (
    <RouteSkeleton
      title="Music at Home"
      description="Core app shell is ready. Feature routes are scaffolded for auth, players, library/search, favorites, and room/device management."
    />
  );
}
