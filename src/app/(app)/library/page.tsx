import { RouteSkeleton } from "../_components/route-skeleton";

export default function LibraryPage() {
  return (
    <RouteSkeleton
      title="Library"
      description="Library browsing scaffold for artists, albums, playlists, and source integrations."
    />
  );
}
